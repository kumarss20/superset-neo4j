"use strict";

exports.__esModule = true;
exports.default = fromHexcodeToCodepoint;

function fromHexcodeToCodepoint(code, joiner) {
  if (joiner === void 0) {
    joiner = '-';
  }

  return code.split(joiner).map(function (point) {
    return parseInt(point, 16);
  });
}