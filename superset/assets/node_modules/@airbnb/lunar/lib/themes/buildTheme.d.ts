import { Theme } from '../types';
export declare type Options = {
    base: string;
    borderRadius?: number;
    boxShadow?: [number, number];
    brand: Theme['color']['brand'];
    color: Theme['color']['core'];
    disabledOpacity?: number;
    fontFamily: string;
    transitionTime?: string;
};
export default function buildTheme(options: Options, accents?: Partial<Theme['color']['accent']>): Theme;
//# sourceMappingURL=buildTheme.d.ts.map