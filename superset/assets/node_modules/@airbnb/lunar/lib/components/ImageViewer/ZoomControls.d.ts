/// <reference types="react" />
export declare const ZOOM_OPTIONS: {
    label: string;
    scale: number;
}[];
export declare type Props = {
    /** The current scale / zoom level. 1 by default. */
    scale?: number;
    /** Callback when scale / zoom changes */
    onScale: (scale: number) => void;
};
/** Zoom controls that can be used with an image viewer component */
export default function ZoomControls(props: Props): JSX.Element;
//# sourceMappingURL=ZoomControls.d.ts.map