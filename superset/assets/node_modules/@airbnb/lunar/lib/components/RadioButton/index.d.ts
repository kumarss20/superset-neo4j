import React from 'react';
import { Props as BaseRadioButtonProps } from '../private/BaseRadioButton';
import { Props as FormFieldProps } from '../FormField';
export declare type Props = Omit<BaseRadioButtonProps, 'value'> & FormFieldProps & {
    /** Top align content. */
    topAlign?: boolean;
    /** Unique value for this radio. */
    value: string;
};
export declare type State = {
    id: string;
};
/** A controlled radio button field. */
export default class RadioButton extends React.Component<Props, State> {
    static defaultProps: {
        button: boolean;
        checked: boolean;
        children: null;
        indeterminate: boolean;
        topAlign: boolean;
    };
    static propTypes: {
        checked: import("prop-types").Requireable<boolean>;
        indeterminate: import("prop-types").Requireable<boolean>;
    };
    state: {
        id: string;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=index.d.ts.map