import React from 'react';
import { Props as BaseCheckBoxProps } from '../private/BaseCheckBox';
import { Props as FormFieldProps } from '../FormField';
export declare type Props = BaseCheckBoxProps & FormFieldProps & {
    /** Top align content. */
    topAlign?: boolean;
};
export declare type State = {
    id: string;
};
/** A controlled checkbox field. */
export default class CheckBox extends React.Component<Props, State> {
    static defaultProps: {
        button: boolean;
        checked: boolean;
        children: null;
        indeterminate: boolean;
        topAlign: boolean;
        value: string;
    };
    static propTypes: {
        checked: import("prop-types").Requireable<boolean>;
        indeterminate: import("prop-types").Requireable<boolean>;
    };
    state: {
        id: string;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=index.d.ts.map