import React from 'react';
import { Props as BaseSelectProps } from '../private/BaseSelect';
import { Props as FormFieldProps } from '../FormField';
export declare type Props = Omit<BaseSelectProps, 'id'> & FormFieldProps & {
    /** Dropdown options. Supports `option` and `optgroup`. */
    children: NonNullable<React.ReactNode>;
    /** Empty and disabled option to display the top of the list. */
    placeholder?: string;
};
export declare type State = {
    id: string;
};
/** A controlled select field. */
export default class Select extends React.Component<Props, State> {
    static propTypes: {
        children: import("prop-types").Validator<string | number | boolean | {} | import("prop-types").ReactElementLike | import("prop-types").ReactNodeArray>;
    };
    static defaultProps: {
        placeholder: string;
    };
    state: {
        id: string;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=index.d.ts.map