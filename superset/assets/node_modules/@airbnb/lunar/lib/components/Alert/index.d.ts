import React from 'react';
export declare type Props = {
    /** Content within the label. */
    children?: React.ReactNode;
    /** Dangerous/failure status (red). */
    danger?: boolean;
    /** Whether to hide the corresponding status icon. */
    hideStatusIcon?: boolean;
    /** Informational status (blue). */
    info?: boolean;
    /** Display the alert as inline. */
    inline?: boolean;
    /** Notice status. */
    notice?: boolean;
    /** Successful status (green). */
    success?: boolean;
    /** The title of the alert, bold text at the top of the box. */
    title: NonNullable<React.ReactNode>;
    /** Warning status (yellow). */
    warning?: boolean;
    /** Callback fired when the alert is closed. */
    onClose?: () => void;
};
/** Classify content through the use of colorful alerts. */
declare function Alert({ children, danger, hideStatusIcon, info, inline, notice, success, title, warning, onClose, }: Props): JSX.Element;
declare namespace Alert {
    var propTypes: {
        danger: import("prop-types").Requireable<boolean>;
        info: import("prop-types").Requireable<boolean>;
        notice: import("prop-types").Requireable<boolean>;
        success: import("prop-types").Requireable<boolean>;
        warning: import("prop-types").Requireable<boolean>;
    };
}
export default Alert;
//# sourceMappingURL=index.d.ts.map