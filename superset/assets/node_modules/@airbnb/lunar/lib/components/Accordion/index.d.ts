import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
import Item from './Item';
export { Item };
export declare type Props = {
    /** Apply a border. */
    bordered?: boolean;
    /** Accordion items and their content. */
    children: NonNullable<React.ReactNode>;
    /** Index of accordion expanded by default. Provide `-1` to collapse all initially. */
    defaultIndex?: number;
};
export declare type State = {
    id: string;
    index: number;
};
/** A controller for multiple accordion items. */
export declare class Accordion extends React.Component<Props & WithStylesProps, State> {
    static defaultProps: {
        bordered: boolean;
        defaultIndex: number;
    };
    state: {
        id: string;
        index: number;
    };
    componentDidUpdate(prevProps: Props): void;
    private handleClick;
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=index.d.ts.map