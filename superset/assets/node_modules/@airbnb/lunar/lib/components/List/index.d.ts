import React from 'react';
import Item from './Item';
export { Item };
export declare type Props = {
    /** List items. */
    children: NonNullable<React.ReactNode>;
    /** Apply gutters between `li`s. */
    gutter?: boolean;
    /** Horizontal list. */
    horizontal?: boolean;
    /** Renders an `<ol></ol>`. */
    ordered?: boolean;
    /** Wrap horizontal list. */
    wrap?: boolean;
};
export default function List({ children, gutter, horizontal, ordered, wrap }: Props): JSX.Element;
//# sourceMappingURL=index.d.ts.map