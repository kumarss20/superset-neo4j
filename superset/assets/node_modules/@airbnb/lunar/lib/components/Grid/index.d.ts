import React from 'react';
import Col from './Col';
export declare type Props = {
    /** Vertically align the columns at the bottom. */
    bottomAlign?: boolean;
    /** Horizontally align the columns to the center. */
    centerAlign?: boolean;
    /** Columns to render. */
    children: NonNullable<React.ReactNode>;
    /** Horizontally align the columns to the end (flex-end). */
    endAlign?: boolean;
    /** Vertically align the columns in the middle. */
    middleAlign?: boolean;
    /** Reverse the order of columns. */
    reversed?: boolean;
    /** Horizontally align the columns to the start (flex-start). */
    startAlign?: boolean;
    /** Vertically align the columns at the top. */
    topAlign?: boolean;
};
/** A grid to contain columns. */
declare function Grid({ bottomAlign, centerAlign, children, endAlign, middleAlign, reversed, startAlign, topAlign, }: Props): JSX.Element;
declare namespace Grid {
    var propTypes: {
        bottomAlign: import("prop-types").Requireable<boolean>;
        centerAlign: import("prop-types").Requireable<boolean>;
        children: import("prop-types").Validator<string | number | boolean | {} | import("prop-types").ReactElementLike | import("prop-types").ReactNodeArray>;
        endAlign: import("prop-types").Requireable<boolean>;
        middleAlign: import("prop-types").Requireable<boolean>;
        startAlign: import("prop-types").Requireable<boolean>;
        topAlign: import("prop-types").Requireable<boolean>;
    };
}
export { Col };
export default Grid;
//# sourceMappingURL=index.d.ts.map