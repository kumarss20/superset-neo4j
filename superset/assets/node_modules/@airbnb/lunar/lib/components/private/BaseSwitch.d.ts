import React from 'react';
import { InputProps } from './FormInput';
export declare type Props = InputProps & {
    /** Whether the switch is checked. */
    checked?: boolean;
    /** Unique identifier. */
    id: string;
    /** Callback fired when the value changes. */
    onChange: (checked: boolean, value: string, event: React.ChangeEvent<HTMLInputElement>) => void;
};
export default function BaseSwitch({ checked, disabled, id, invalid, onChange, ...restProps }: Props): JSX.Element;
//# sourceMappingURL=BaseSwitch.d.ts.map