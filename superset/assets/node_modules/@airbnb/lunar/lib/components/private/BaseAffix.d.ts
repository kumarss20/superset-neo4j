import React from 'react';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** @ignore */
    after?: boolean;
    /** @ignore */
    before?: boolean;
    /** Content within the affix. */
    children: NonNullable<React.ReactNode>;
    /** Decrease font size and padding. */
    compact?: boolean;
    /** Mark the affix as disabled. */
    disabled?: boolean;
    /** Align using flexbox. */
    flex?: boolean;
};
export default class BaseAffix extends React.PureComponent<Props & WithStylesProps> {
    static propTypes: {
        after: import("prop-types").Requireable<boolean>;
        before: import("prop-types").Requireable<boolean>;
    };
    static defaultProps: {
        after: boolean;
        before: boolean;
        compact: boolean;
        disabled: boolean;
        flex: boolean;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=BaseAffix.d.ts.map