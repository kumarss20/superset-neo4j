import { StyleSheet } from 'aesthetic';
import { Theme } from '../../types';
export default function datePickerStyles({ color, font, pattern, transition, ui, unit, }: Theme): StyleSheet;
//# sourceMappingURL=datePickerStyles.d.ts.map