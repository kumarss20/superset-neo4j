import React from 'react';
import { WithStylesProps } from '../../../composers/withStyles';
export declare type Props = {
    children?: React.ReactNode;
    /** True removes the background. */
    noBackground: boolean;
    /** Callback for when the overlay should be closed. */
    onClose: () => void;
    /** Callback for when the window is resized. */
    onResize: () => void;
    /** X position of content. */
    x?: number;
    /** Y position of content. */
    y?: number;
};
export declare type State = {
    height: number;
};
export declare class Portal extends React.Component<Props & WithStylesProps, State> {
    state: {
        height: number;
    };
    ref: React.RefObject<HTMLDivElement>;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private handleClick;
    private handleKeyDown;
    private handleResize;
    private handleResizeThrottled;
    private handleScroll;
    private handleScrollThrottled;
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=index.d.ts.map