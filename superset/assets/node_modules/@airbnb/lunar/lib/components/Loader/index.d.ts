/// <reference types="react" />
export declare type Props = {
    /** Display inline instead of absolutely positioned. */
    inline?: boolean;
    /** Invert colors. */
    inverted?: boolean;
    /** Increase the dot size. */
    large?: boolean;
    /** Position statically instead of absolutely. */
    static?: boolean;
};
/** A small 3-dot loading indicator. */
export default function Loader({ inline, inverted, large, static: isStatic }: Props): JSX.Element;
//# sourceMappingURL=index.d.ts.map