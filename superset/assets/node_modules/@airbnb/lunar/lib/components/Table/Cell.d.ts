import React from 'react';
export declare type Props = {
    /** Display in the center horizontally. */
    centerAlign?: boolean;
    /** Content to render. */
    children?: React.ReactNode;
    /** Span multiple columns. */
    colSpan?: number;
    /** Display horizontally at the end. */
    endAlign?: boolean;
    /** Render as a table header. */
    header?: boolean;
    /** Display horizontally at the start. */
    startAlign?: boolean;
    /** Truncate text and display all on hover. */
    truncate?: boolean;
    /** Wrap text and white space. */
    wrap?: boolean;
};
/** An individual table cell. */
export default function TableCell({ centerAlign, children, header, startAlign, endAlign, truncate, wrap, ...props }: Props): JSX.Element;
//# sourceMappingURL=Cell.d.ts.map