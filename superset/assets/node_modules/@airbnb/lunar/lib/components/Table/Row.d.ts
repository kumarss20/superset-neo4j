import React from 'react';
export declare type Props = {
    /** Table cells to render. */
    children: NonNullable<React.ReactNode>;
    /** Dangerous/failure row (red). */
    danger?: boolean;
    /** Informational row (blue). */
    info?: boolean;
    /** Muted/disabled row (gray). */
    muted?: boolean;
    /** Notice row. */
    notice?: boolean;
    /** Successful row (green). */
    success?: boolean;
    /** Warning row (yellow). */
    warning?: boolean;
};
/** A table row that contains multiple cells. */
declare function TableRow({ children, danger, info, muted, notice, success, warning, ...props }: Props): JSX.Element;
declare namespace TableRow {
    var propTypes: {
        danger: import("prop-types").Requireable<boolean>;
        info: import("prop-types").Requireable<boolean>;
        muted: import("prop-types").Requireable<boolean>;
        notice: import("prop-types").Requireable<boolean>;
        success: import("prop-types").Requireable<boolean>;
        warning: import("prop-types").Requireable<boolean>;
    };
}
export default TableRow;
//# sourceMappingURL=Row.d.ts.map