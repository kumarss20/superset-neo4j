import { NavbarElementProps as NavBarProps } from 'react-day-picker';
export declare type Props = NavBarProps & {
    /** Callback for a reset button. */
    onResetClick?: () => void;
    /** Show the reset button. */
    showResetButton?: boolean;
    /** Whether there is a footer to account for positioning. */
    noFooter?: boolean;
};
declare const _default: import("aesthetic-react").StyledComponent<import("../../../types").Theme, NavBarProps & {
    /** Callback for a reset button. */
    onResetClick?: (() => void) | undefined;
    /** Show the reset button. */
    showResetButton?: boolean | undefined;
    /** Whether there is a footer to account for positioning. */
    noFooter?: boolean | undefined;
} & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=NavBar.d.ts.map