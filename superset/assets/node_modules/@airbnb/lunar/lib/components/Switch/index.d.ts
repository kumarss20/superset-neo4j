import React from 'react';
import { Props as BaseSwitchProps } from '../private/BaseSwitch';
import { Props as FormFieldProps } from '../FormField';
export declare type Props = Omit<BaseSwitchProps, 'id'> & FormFieldProps;
export declare type State = {
    id: string;
};
/** A controlled switch (fancy checkbox) field. */
export default class Switch extends React.Component<Props, State> {
    static defaultProps: {
        checked: boolean;
    };
    state: {
        id: string;
    };
    render(): JSX.Element;
}
//# sourceMappingURL=index.d.ts.map