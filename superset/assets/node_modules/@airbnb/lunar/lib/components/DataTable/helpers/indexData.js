"use strict";

exports.__esModule = true;
exports.indexData = void 0;

var _memoize = _interopRequireDefault(require("lodash/memoize"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

// eslint-disable-next-line import/prefer-default-export
const indexData = (0, _memoize.default)(dataList => {
  const indexedDataList = [];
  dataList.forEach((row, idx) => {
    const children = [];

    if (row.metadata && row.metadata.children && row.metadata.children.length > 0) {
      row.metadata.children.forEach((child, childIdx) => {
        children.push(_extends({}, child, {
          metadata: _extends({}, child.metadata, {
            originalIndex: childIdx,
            isChild: true
          })
        }));
      });
    }

    indexedDataList.push(_extends({}, row, {
      metadata: _extends({}, row.metadata, {
        children,
        originalIndex: idx,
        isChild: false
      })
    }));
  });
  return indexedDataList;
});
exports.indexData = indexData;