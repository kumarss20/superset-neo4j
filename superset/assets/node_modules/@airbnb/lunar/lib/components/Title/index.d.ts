import React from 'react';
export declare type Props = {
    /** Align the text in the center. */
    centerAlign?: boolean;
    /** The text to render. */
    children?: React.ReactNode;
    /** Align the text on the end. */
    endAlign?: boolean;
    /** Render the text inline instead of block. */
    inline?: boolean;
    /** Invert text colors. */
    inverted?: boolean;
    /** Heading level of the text, with 1 being the greatest. */
    level: 1 | 2 | 3;
    /** Mark the text as muted. */
    muted?: boolean;
    /** Render with primary color text. */
    primary?: boolean;
};
/** Display a string of text as a heading and or section title. */
declare function Title({ centerAlign, children, endAlign, inline, inverted, level, muted, primary, }: Props): JSX.Element;
declare namespace Title {
    var propTypes: {
        centerAlign: import("prop-types").Requireable<boolean>;
        endAlign: import("prop-types").Requireable<boolean>;
        inverted: import("prop-types").Requireable<boolean>;
        muted: import("prop-types").Requireable<boolean>;
        primary: import("prop-types").Requireable<boolean>;
    };
}
export default Title;
//# sourceMappingURL=index.d.ts.map