/// <reference types="react" />
import { SpacingRange } from '../Spacing';
export declare type Props = {
    /** Spacing on the bottom. */
    bottom?: SpacingRange;
    /** Render the divider with a short width. */
    short?: boolean;
    /** Spacing on the top. */
    top?: SpacingRange;
};
/** A horizontal divider. */
export default function Divider({ bottom, short, top }: Props): JSX.Element;
//# sourceMappingURL=index.d.ts.map