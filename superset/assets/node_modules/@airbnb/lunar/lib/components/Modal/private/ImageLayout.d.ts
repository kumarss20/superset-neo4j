import React from 'react';
export declare const MAX_HEIGHT_IMAGE = 720;
export declare const MAX_HEIGHT_IMAGE_SMALL = 420;
export declare type ModalImageConfig = {
    /** Responsive image sizes. Corresponds to `imageSrcSet`. */
    sizes?: string[];
    /** Responsive image source set. Corresponds to `imageSizes`. */
    srcSet?: string[];
    /** Should the image be centered, or should it cover its container? */
    type: 'center' | 'cover';
    /** Source of the image. */
    url: string;
};
export declare type Props = ModalImageConfig & {
    /** Left pane content. */
    children: NonNullable<React.ReactNode>;
};
export default function ModalImageLayout({ children, sizes, srcSet, type, url }: Props): JSX.Element;
//# sourceMappingURL=ImageLayout.d.ts.map