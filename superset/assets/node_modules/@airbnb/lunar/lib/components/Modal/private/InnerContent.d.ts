import React from 'react';
export declare type Props = {
    /** Dialog content. */
    children: NonNullable<React.ReactNode>;
    /** Footer content. */
    footer?: React.ReactNode;
    /** Show the large version of the Dialog. */
    large?: boolean;
    /** Modal content height becomes scrollable. */
    scrollable?: boolean;
    /** Show the small version of the Dialog. */
    small?: boolean;
    /** Dialog header subtitle. */
    subtitle?: React.ReactNode;
    /** Dialog header title. */
    title?: React.ReactNode;
    /** Callback for when the Dialog should be closed.  */
    onClose: (event: React.MouseEvent | React.KeyboardEvent) => void;
};
/** A Dialog component with a backdrop and a standardized layout. */
export default function ModalInnerContent({ children, footer, large, small, onClose, scrollable, subtitle, title, }: Props): JSX.Element;
//# sourceMappingURL=InnerContent.d.ts.map