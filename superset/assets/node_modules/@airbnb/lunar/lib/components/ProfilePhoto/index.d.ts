import React from 'react';
import PropTypes from 'prop-types';
import { WithStylesProps } from '../../composers/withStyles';
export declare type Props = {
    /** Fallback image if the image is broken. */
    fallbackImageSrc?: string;
    /** URL of the image to display. */
    imageSrc: string;
    /** Whether the component should be inline. */
    inline?: boolean;
    /** Large size (96px). */
    large?: boolean;
    /** Macro size (160px). */
    macro?: boolean;
    /** Size in units to multiply by. */
    size?: number;
    /** Small size (24px). */
    small?: boolean;
    /** Whether the image should be square (it is rounded by default). */
    square?: boolean;
    /** Accessibility text for the Photo. */
    title: string;
};
declare type State = {
    src: string;
};
/** Display a profile photo. */
export declare class ProfilePhoto extends React.Component<Props & WithStylesProps, State> {
    static propTypes: {
        large: PropTypes.Requireable<any>;
        macro: PropTypes.Requireable<any>;
        size: PropTypes.Requireable<any>;
        small: PropTypes.Requireable<any>;
    };
    static defaultProps: {
        inline: boolean;
        square: boolean;
    };
    state: {
        src: string;
    };
    componentDidUpdate(prevProps: Props & WithStylesProps): void;
    private handleError;
    render(): JSX.Element;
}
declare const _default: import("aesthetic-react").StyledComponent<import("../../types").Theme, Props & import("aesthetic-react").WithStylesWrapperProps>;
export default _default;
//# sourceMappingURL=index.d.ts.map