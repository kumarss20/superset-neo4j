import React from 'react';
import { Props as LightboxImageProps } from './Image';
export declare type Props = {
    /** Images to show. */
    images: LightboxImageProps[];
    /** Image start index. */
    startIndex?: number;
    /** Show zoom controls. */
    showZoomControls?: boolean;
    /** Show rotate controls. */
    showRotateControls?: boolean;
    /** Function to run on close. */
    onClose: () => void;
};
export declare type State = {
    activeIndex: number;
    hideAside: boolean;
    scale: number;
    rotation: number;
};
export default class Lightbox extends React.PureComponent<Props, State> {
    static defaultProps: {
        startIndex: number;
    };
    state: {
        activeIndex: number;
        hideAside: boolean;
        scale: number;
        rotation: number;
    };
    preloadedUrls: Map<string, boolean | HTMLImageElement>;
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    preloadImages(): void;
    private handleChangeSlide;
    private handleToggleAside;
    private handleZoomImage;
    private handleRotateImage;
    render(): JSX.Element;
}
//# sourceMappingURL=index.d.ts.map