import React from 'react';
import Separator from './Separator';
import Item from './Item';
import Row from './Row';
export declare type Props = {
    /** List of `Row`s, `Item`s, and `Separator`s to render in the menu. */
    children?: React.ReactNode;
    /** Accessibility label. */
    accessibilityLabel: string;
    /** Maximum height of the menu before scrolling. */
    maxHeight?: number;
    /** Minimum width of the menu. */
    minWidth?: number;
    /** Accessibility role. */
    role?: string;
    /** Whether or not the menu has visible overflow. */
    overflow?: boolean;
};
/** An abstract menu for use within dropdowns, selects, autocompletes, and more. */
export default function Menu({ accessibilityLabel, children, maxHeight, minWidth, overflow, role, }: Props): JSX.Element;
export { Item, Separator, Row };
//# sourceMappingURL=index.d.ts.map