/// <reference types="react" />
import { InterweaveProps as BaseInterweaveProps, MatcherInterface, FilterInterface } from 'interweave';
import { EmailMatcher, UrlMatcher } from 'interweave-autolink';
import { EmojiMatcher } from 'interweave-emoji';
export declare const globalMatchers: MatcherInterface<{}>[];
export declare const globalFilters: FilterInterface[];
export declare const urlMatcher: UrlMatcher;
export declare const emailMatcher: EmailMatcher;
export declare const emojiMatcher: EmojiMatcher;
export declare const emojiMatcherWithEmoticons: EmojiMatcher;
export declare type Props = BaseInterweaveProps & {
    /** Render any found links using large prop. */
    large?: boolean;
    /** Only run these matchers (by name). */
    onlyMatchers?: string[];
    /** Render any found links using small prop. */
    small?: boolean;
    /** Convert emoticons to emojis. */
    withEmoticons?: boolean;
};
/**
 * Safely render HTML, filter attributes, autowrap text with matchers, render emoji characters,
 * and much more.
 */
export default function Interweave({ content, filters, matchers, onlyMatchers, withEmoticons, ...props }: Props): JSX.Element;
//# sourceMappingURL=index.d.ts.map