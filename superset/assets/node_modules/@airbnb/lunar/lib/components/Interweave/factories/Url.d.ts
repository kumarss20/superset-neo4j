/// <reference types="react" />
import { UrlProps } from 'interweave-autolink';
export declare type Props = UrlProps & {
    /** Render using large prop on the Link component. */
    large?: boolean;
    /** Render using small prop on the Link component. */
    small?: boolean;
};
export default function Url({ children, href, large, newWindow, small, url, }: Props): JSX.Element;
//# sourceMappingURL=Url.d.ts.map