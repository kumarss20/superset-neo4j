"use strict";

exports.__esModule = true;
exports.default = defaultFormatter;

var _Translate = _interopRequireDefault(require("../Translate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function defaultFormatter(chosen, getLabel) {
  if (chosen.length > 0) {
    const labels = chosen.map((_, i, a) => getLabel(a.slice(0, i + 1)));
    return labels.join(' > ');
  }

  return _Translate.default.phrase('Select from hierarchy', {}, {
    context: 'Placeholder text for HierarchyPicker',
    key: 'lunar.picker.selectPlaceholder'
  });
}