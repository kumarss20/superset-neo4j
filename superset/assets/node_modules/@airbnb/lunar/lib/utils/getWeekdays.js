"use strict";

exports.__esModule = true;
exports.default = getWeekdays;

var _luxon = require("luxon");

var _ = _interopRequireDefault(require(".."));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getWeekdays(type, sundayFirst) {
  if (sundayFirst === void 0) {
    sundayFirst = false;
  }

  const days = [..._luxon.Info.weekdays(type, {
    locale: _.default.locale()
  })];

  if (sundayFirst) {
    const sunday = days.pop();
    days.unshift(sunday);
  }

  return days;
}