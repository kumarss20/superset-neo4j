"use strict";

exports.__esModule = true;
exports.default = filterEmojis;

var _emojibase = require("emojibase");

var _emojibaseRegex = _interopRequireDefault(require("emojibase-regex"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const EMOJI_REGEX = new RegExp(_emojibaseRegex.default.source, 'g');

function filterEmojis(content, allowed) {
  if (allowed === void 0) {
    allowed = [];
  }

  const whitelist = new Set(allowed);
  return content.replace(EMOJI_REGEX, unicode => whitelist.has((0, _emojibase.fromUnicodeToHexcode)(unicode)) ? unicode : '');
}