import { DateTime } from 'luxon';
import { DateTimeType } from '../types';
export declare type Options = {
    locale?: string;
    sourceFormat?: string;
    timezone?: string | boolean;
};
export { DateTime };
export default function createDateTime(value?: DateTimeType, { locale, sourceFormat, timezone }?: Options): DateTime;
//# sourceMappingURL=createDateTime.d.ts.map