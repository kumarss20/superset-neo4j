import { Currency, Locale, PriceType } from '../types';
export declare type Options = {
    display?: PriceType;
    precision?: number;
    round?: boolean;
    trimTrailingZeros?: boolean;
    locale?: Locale;
};
export default function formatPrice(price: number | string, currency?: Currency, options?: Options): string;
//# sourceMappingURL=formatPrice.d.ts.map