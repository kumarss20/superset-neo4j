"use strict";

exports.__esModule = true;
exports.default = void 0;

var _aestheticAdapterAphrodite = _interopRequireDefault(require("aesthetic-adapter-aphrodite"));

var _luxon = require("luxon");

var _global = _interopRequireDefault(require("./themes/global"));

var _light = _interopRequireDefault(require("./themes/light"));

var _dark = _interopRequireDefault(require("./themes/dark"));

var _getLocaleFromClient = _interopRequireDefault(require("./utils/getLocaleFromClient"));

var _getTimezoneFromClient = _interopRequireDefault(require("./utils/getTimezoneFromClient"));

var _constants = require("./constants");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

class Core {
  constructor() {
    _defineProperty(this, "settings", {
      defaultLocale: (0, _getLocaleFromClient.default)() || _constants.DEFAULT_LOCALE,
      defaultTimezone: (0, _getTimezoneFromClient.default)() || _constants.DEFAULT_TIMEZONE,
      emojiCDN: '',
      errorURL: '',
      fontFaces: {},
      fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"',
      logger: null,
      name: 'Lunar',
      rtl: false,
      theme: 'light',
      translator: null,
      translatorComponent: null
    });

    _defineProperty(this, "aesthetic", new _aestheticAdapterAphrodite.default([], {
      theme: 'light'
    }));

    _defineProperty(this, "log", (error, extra) => {
      const {
        logger
      } = this.settings;

      if (logger) {
        logger(error, extra);
      }
    });

    _defineProperty(this, "translate", (phrase, baseParams, options) => {
      const {
        translator
      } = this.settings;

      if (translator) {
        return translator(phrase, baseParams, options);
      }

      const params = baseParams || {};
      let message = phrase;

      if (phrase.includes('||||')) {
        const [singular, plural] = phrase.split('||||');
        const count = params.smartCount || 0;
        message = count === 1 ? singular : plural;
      } // Low-level token interpolation


      return message.replace(/%\{(\w+)\}/g, (match, key) => "" + params[key]);
    });
  }

  initialize(settings) {
    this.settings = _extends({}, this.settings, {}, settings);
    this.bootstrapAesthetic();
    this.bootstrapLuxon();
  }

  bootstrapAesthetic() {
    const {
      fontFaces,
      rtl,
      theme
    } = this.settings;
    const fontFamily = this.fontFamily();
    const globals = (0, _global.default)(fontFaces);

    try {
      this.aesthetic.registerTheme('light', (0, _light.default)(fontFamily), globals).registerTheme('dark', (0, _dark.default)(fontFamily), globals) // Aesthetic's ThemeContext default theme is "default",
      // so let's register a default theme based on light
      // so that downstream consumers don't break.
      .extendTheme('default', 'light', {});
    } catch (_unused) {// Tests trigger an error, so ignore it
    }

    this.aesthetic.options.rtl = rtl;
    this.aesthetic.options.theme = theme;
  }

  bootstrapLuxon() {
    _luxon.Settings.defaultLocale = this.locale();
    _luxon.Settings.defaultZoneName = this.timezone();
    _luxon.Settings.throwOnInvalid = true;
  }

  fontFamily() {
    const locale = this.locale();

    if (locale.startsWith('ja')) {
      return '"ヒラギノ角ゴ Pro", "Hiragino Kaku Gothic Pro", メイリオ, Meiryo, Osaka, "ＭＳ Ｐゴシック", "MS PGothic", "MS Gothic", "ＭＳ ゴシック", "Helvetica Neue", Helvetica, Arial, sans-serif';
    }

    if (locale.startsWith('ko')) {
      return '"나눔 고딕", "Nanum Gothic", "맑은 고딕", "Malgun Gothic", "Apple Gothic", 돋움, Dotum, "Helvetica Neue", Helvetica, Arial, sans-serif';
    }

    if (locale.includes('zh')) {
      return '"Hiragino Sans GB", 华文细黑, STHeiti, 微软雅黑, "Microsoft YaHei", SimHei, "Helvetica Neue", Helvetica, Arial, sans-serif';
    }

    return this.settings.fontFamily;
  }

  isRTL(context) {
    if (context && context !== 'neutral') {
      return context === 'rtl';
    } // If undefined or neutral, fallback to the global setting


    return this.settings.rtl;
  }

  locale() {
    return this.settings.defaultLocale || _constants.DEFAULT_LOCALE;
  }

  timezone() {
    return this.settings.defaultTimezone || _constants.DEFAULT_TIMEZONE;
  }

}

const instance = new Core();

if (process.env.NODE_ENV === 'test') {
  // eslint-disable-next-line
  const {
    TestAesthetic
  } = require('aesthetic/lib/testUtils'); // @ts-ignore Allow mutation of readonly for testing


  instance.aesthetic = new TestAesthetic({
    theme: 'light'
  });
}

var _default = instance;
exports.default = _default;