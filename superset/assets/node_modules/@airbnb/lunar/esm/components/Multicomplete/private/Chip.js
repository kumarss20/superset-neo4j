import _pt from "prop-types";

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import React from 'react';
import IconCloseAlt from '@airbnb/lunar-icons/lib/interface/IconCloseAlt';
import Chip from '../../Chip';
export default class MulticompleteChip extends React.Component {
  constructor() {
    super(...arguments);

    _defineProperty(this, "handleClick", event => {
      this.props.onClick(this.props.value, event);
    });
  }

  render() {
    const {
      value
    } = this.props;
    return React.createElement(Chip, {
      afterIcon: React.createElement(IconCloseAlt, {
        decorative: true,
        size: "2em"
      }),
      onIconClick: this.handleClick
    }, value);
  }

}

_defineProperty(MulticompleteChip, "propTypes", {
  value: _pt.string.isRequired,
  onClick: _pt.func.isRequired
});