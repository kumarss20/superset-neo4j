import _pt from "prop-types";
import React, { useState } from 'react';
import useStyles from '../../hooks/useStyles';
import SheetContext from './SheetContext';
import { styleSheetSheetArea as styleSheet } from './styles';

/** Container in which to render sheets. */
export default function SheetArea(_ref) {
  let {
    children
  } = _ref;
  const [styles, cx] = useStyles(styleSheet);
  const [visible, setVisible] = useState(false);

  const setSheetVisible = nextVisible => {
    setVisible(nextVisible);
  };

  return React.createElement(SheetContext.Provider, {
    value: setSheetVisible
  }, React.createElement("div", {
    className: cx(visible && styles.sheet_visible)
  }, children));
}
SheetArea.propTypes = {
  children: _pt.any.isRequired
};