function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

import React from 'react';
import EmojiPicker from '../EmojiPicker';
import { EMOJI_WHITELIST } from '../../constants';
/**
 * Display an emoji picker that utilizes a restricted whitelist of acceptable emojis.
 */

export default function EmojiRestrictedPicker(props) {
  return React.createElement(EmojiPicker, _extends({
    disableCommonlyUsed: true,
    disableGroups: true,
    disableSearch: true,
    disableSkinTones: true,
    hideEmoticon: true,
    hideGroupHeaders: true,
    hideShortcodes: true,
    emojiLargeSize: 36
  }, props, {
    rowCount: 2,
    stickyGroupHeader: false,
    allowList: EMOJI_WHITELIST
  }));
}