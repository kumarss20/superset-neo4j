import _pt from "prop-types";
import React from 'react';
import useStyles from '../../hooks/useStyles';
import { styleSheet } from './styles';

/** Horizontally align `Button`s with a consistent gutter between each. */
export default function ButtonGroup(_ref) {
  let {
    children,
    endAlign,
    stacked
  } = _ref;
  const [styles, cx] = useStyles(styleSheet);
  return React.createElement("div", {
    className: cx(styles.buttonGroup, endAlign && styles.buttonGroup_endAlign, stacked && styles.buttonGroup_stacked)
  }, React.Children.map(children, child => child ? React.createElement("div", {
    className: cx(stacked ? styles.cell_stacked : styles.cell)
  }, child) : null));
}
ButtonGroup.propTypes = {
  children: _pt.any.isRequired,
  endAlign: _pt.bool,
  stacked: _pt.bool
};