function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

import React from 'react';
import Link from '../Link';
const StyledSecondaryLink = Link.extendStyles((_ref) => {
  let {
    color
  } = _ref;
  return {
    link: {
      color: color.core.neutral[5],
      ':hover': {
        color: color.core.neutral[6]
      }
    }
  };
});
/** A link to use for secondary actions. Supports all the same props as `Link`. */

export default function SecondaryLink(_ref2) {
  let {
    children
  } = _ref2,
      props = _objectWithoutPropertiesLoose(_ref2, ["children"]);

  return React.createElement(StyledSecondaryLink, props, children);
}