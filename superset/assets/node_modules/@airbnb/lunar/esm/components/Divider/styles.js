const styleSheet = (_ref) => {
  let {
    ui,
    unit
  } = _ref;
  return {
    divider: {
      borderBottom: ui.border
    },
    divider_short: {
      width: 4 * unit
    }
  };
};

export default styleSheet;
export { styleSheet };