import React from 'react';
import Text from '../Text';
export default function DefaultRenderer(_ref) {
  let {
    row,
    keyName
  } = _ref;
  const content = row.rowData.data[keyName];
  return React.createElement(Text, null, typeof content === 'string' || typeof content === 'number' ? content : null);
}