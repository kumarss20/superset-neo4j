import { Info } from 'luxon';
import Core from '..';
export default function getWeekdays(type, sundayFirst) {
  if (sundayFirst === void 0) {
    sundayFirst = false;
  }

  const days = [...Info.weekdays(type, {
    locale: Core.locale()
  })];

  if (sundayFirst) {
    const sunday = days.pop();
    days.unshift(sunday);
  }

  return days;
}