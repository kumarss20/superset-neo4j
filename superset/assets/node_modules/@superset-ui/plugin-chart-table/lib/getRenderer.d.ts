/// <reference types="react" />
import { RendererProps } from '@airbnb/lunar/lib/components/DataTable/types';
import { NumberFormatter } from '@superset-ui/number-format';
import { TimeFormatter } from '@superset-ui/time-format';
export declare type ColumnType = {
    key: string;
    label: string;
    format?: NumberFormatter | TimeFormatter | undefined;
    type: 'metric' | 'string';
    maxValue?: number;
    minValue?: number;
};
export declare type Cell = {
    key: string;
    value: any;
};
export default function getRenderer({ column, alignPositiveNegative, colorPositiveNegative, enableFilter, isSelected, handleCellSelected, }: {
    column: ColumnType;
    alignPositiveNegative: boolean;
    colorPositiveNegative: boolean;
    enableFilter: boolean;
    isSelected: (cell: Cell) => boolean;
    handleCellSelected: (cell: Cell) => (...args: any[]) => void;
}): ({ keyName, row }: RendererProps<import("@airbnb/lunar/lib/components/DataTable/types").RowData>) => JSX.Element;
