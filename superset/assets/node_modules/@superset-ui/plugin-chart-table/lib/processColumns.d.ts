import { NumberFormatter } from '@superset-ui/number-format';
import { TimeFormatter } from '@superset-ui/time-format';
import { PlainObject } from './types';
declare type inputType = {
    columns: string[];
    metrics: string[];
    records: any[];
    tableTimestampFormat: string;
    datasource: PlainObject;
};
declare const getCreateSelectorFunction: () => import("reselect").OutputSelector<inputType, {
    format: TimeFormatter | NumberFormatter | undefined;
    key: string;
    label: any;
    type: "string" | "metric";
}[], (res1: string[], res2: string[], res3: any[], res4: string, res5: PlainObject) => {
    format: TimeFormatter | NumberFormatter | undefined;
    key: string;
    label: any;
    type: "string" | "metric";
}[]>;
export default getCreateSelectorFunction;
